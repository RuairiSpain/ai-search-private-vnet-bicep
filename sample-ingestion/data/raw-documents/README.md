# Raw documents for notebook upload

Place sample files here before running the notebook upload cell.

Supported examples:

- `.pdf`
- `.docx`
- `.pptx`
- `.png`, `.jpg`, `.jpeg`, `.tif`, `.tiff` for OCR/image-heavy scenarios

The notebook uploads all matching files to Azure Blob Storage for indexer-driven cracking, chunking and vectorisation.
